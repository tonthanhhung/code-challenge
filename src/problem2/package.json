{
  "name": "problem2-fancy-form",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://tonthanhhung.github.io/code-challenge/currency-swap",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "predeploy": "npm run build && npm run build:deploy",
    "build:deploy": "rm -rf deploy && mkdir -p deploy/currency-swap && cp -r dist/* deploy/currency-swap/ && touch deploy/.nojekyll",
    "deploy": "gh-pages -d deploy -b gh-pages"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/node": "^24.12.2",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "eslint": "^9.39.4",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "gh-pages": "^6.3.0",
    "globals": "^17.4.0",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.58.0",
    "vite": "^8.0.4"
  }
}
